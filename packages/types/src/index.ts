// Example shared type
export type UserProfile = {
  id: string;
  email: string;
  name?: string;
};

// More types can be added here as the project grows.
console.log('Shared types package loaded');
